{
  "name": "threejs-galaxy-shader",
  "version": "0.9.0",
  "description": "A efficient galaxy rendered for the web.",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/esm/index.js",
      "require": "./dist/common/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build && tsc --build -f tsconfig.commonjs.json",
    "build:watch": "tsc --build --watch && tsc --build -f tsconfig.commonjs.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "threejs",
    "galaxy",
    "shader"
  ],
  "peerDependencies": {
    "three": "^0.136.0"
  },
  "author": "Amit Digga",
  "license": "ISC",
  "devDependencies": {
    "@types/three": "^0.179.0",
    "typescript": "^5.9.2"
  }
}
